[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
pythonpath = backend
python_files = tests.py test_*.py *_tests.py
addopts = --ds=config.settings.test
markers =
    tenant: tenant isolation tests
    permissions: role and membership tests
    inventory: stock operation tests
    transactions: transaction safety tests
    invites: workspace invite tests
